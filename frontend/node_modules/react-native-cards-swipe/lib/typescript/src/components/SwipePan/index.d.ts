import * as React from 'react';
export declare enum SWIPE_DIRECTION {
    LEFT = "left",
    RIGHT = "right",
    DEFAULT = "default"
}
interface Value {
    value: number;
}
interface Props {
    x: Value;
    y: Value;
    originY: Value;
    onSnap: (swipedRight: boolean) => void;
    onStart: () => void;
    onEnd: () => void;
    onChangeDirection: (direction: SWIPE_DIRECTION) => void;
    children: React.ReactNode;
}
declare const SwipePan: ({ x, y, onSnap, onStart, onChangeDirection, onEnd, originY, children, }: Props) => JSX.Element;
export default SwipePan;
